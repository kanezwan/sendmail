# 📧 发票邮件自动处理工具

自动从邮箱中提取"票通"发送的电子发票邮件，下载 PDF 附件并转换为 JPG 图片，按 `{姓名}-{地区}` 分目录保存。

## 功能概述

1. **多邮箱支持**：同时处理多个邮箱（163 邮箱、QQ 邮箱等）
2. **自动解析**：从邮件主题提取地区信息，从 PDF 文件名提取姓名
3. **PDF → JPG**：自动将发票 PDF 转换为高清 JPG 图片
4. **智能跳过**：已下载的发票不会重复处理
5. **自动清理**：发票邮件处理完成后自动从邮箱中删除，非发票邮件保留不动

## 目录结构

```
sendinvoice/
├── .env                 # 邮箱账号配置（需自行填写）
├── .gitignore
├── requirements.txt     # Python 依赖
├── main.py              # 主程序
├── doc/                 # 需求文档
└── invoice/             # 发票图片输出目录（自动创建）
    ├── 张三-广州/
    │   ├── 264420000036_92402671.jpg
    │   └── 264420000036_92723731.jpg
    ├── 李四-深圳市/
    │   └── 269520000013_85413906.jpg
    └── ...
```

## 安装

### 1. 安装 Python 依赖

```bash
pip install -r requirements.txt
```

依赖包：
- `pypdfium2` — PDF 渲染引擎（纯 Python，无需系统依赖）
- `Pillow` — 图片处理

### 2. 配置邮箱账号

编辑项目根目录下的 `.env` 文件：

```env
# 多个账号用分号（;）分隔
# 格式: 用户名:密码(授权码):IMAP服务器
EMAIL_ACCOUNTS=你的邮箱:授权码:imap服务器地址
```

**示例：**

```env
# 单个邮箱
EMAIL_ACCOUNTS=example@163.com:YOUR_AUTH_CODE:imap.163.com

# 多个邮箱
EMAIL_ACCOUNTS=user1@163.com:AUTH_CODE_1:imap.163.com;user2@qq.com:AUTH_CODE_2:imap.qq.com
```

### 3. 获取邮箱授权码

| 邮箱 | IMAP 服务器 | 授权码获取方式 |
|------|------------|--------------|
| 163 邮箱 | `imap.163.com` | 登录 [mail.163.com](https://mail.163.com) → 设置 → POP3/SMTP/IMAP → 开启 IMAP 服务 → 获取授权码 |
| QQ 邮箱 | `imap.qq.com` | 登录 [mail.qq.com](https://mail.qq.com) → 设置 → 账户 → 开启 IMAP/SMTP 服务 → 获取授权码 |

> ⚠️ **注意**：IMAP 登录需要使用**授权码**，不是邮箱登录密码。

## 使用方法

```bash
python main.py
```

程序会自动执行以下流程：

```
连接邮箱 → 读取收件箱 → 筛选发票邮件 → 下载 PDF 附件 → 转换为 JPG → 按目录保存
```

### 运行示例

```
📧 正在连接邮箱 example@163.com (imap.163.com) ...
  ✅ 登录成功
  📂 收件箱打开成功，共 20 封邮件
  📬 找到 20 封邮件，开始处理...

── 邮件: 您收到一张来自广州有限公司的电子发票【发票金额：58.00】
   姓名: user | 地区: 广州
  📥 已下载: 264420000036_71_user.pdf
  ✅ 已转换: 264420000036_72.jpg

==================================================
✅ 全部完成！共处理 13 封发票邮件，跳过 7 封非发票邮件
📁 发票图片保存在: /path/to/sendinvoice/invoice
```

## 邮件格式说明

本工具处理的邮件需满足以下格式：

- **邮件主题**：`您收到一张来自{地区}{公司名称}的电子发票【发票金额：xxx】`
- **PDF 附件名**：`{发票代码}_{发票号码}_{姓名}.pdf`

程序会自动提取地区和姓名，生成目录 `{姓名}-{地区}/`，文件名为 `{发票代码}_{发票号码}.jpg`。
